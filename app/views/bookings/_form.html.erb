<%= simple_form_for([@pro, @booking]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  <%= f.input :genre, label: 'Type' %>
  <%= f.input :date, :as => :date   %>
  <%= f.input :time, :as => :time  %>
  <%= f.input :location %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
